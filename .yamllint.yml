extends: default

ignore: |
  automation/ansible/inventories/lab/vault.yml
  .venv/

rules:
  document-start: disable
  line-length:
    max: 160
    level: warning
  truthy:
    allowed-values:
      - 'true'
      - 'false'
    check-keys: false